---
phase: 15-skill-entry-point
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/visual-orchestrator/SKILL.md
  - CLAUDE.md
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "User can discover the visual-orchestrator skill by reading CLAUDE.md task routing table"
    - "User can invoke the skill by following SKILL.md 3-step workflow"
    - "SKILL.md handles missing Script.md with a clear error message pointing to the writer skill"
    - "CLAUDE.md folder map, pipeline skills, and What to Load tables all reference visual-orchestrator"
  artifacts:
    - path: ".claude/skills/visual-orchestrator/SKILL.md"
      provides: "Skill invocation workflow"
      contains: "visual-orchestrator"
    - path: "CLAUDE.md"
      provides: "Updated routing tables"
      contains: "visual-orchestrator"
  key_links:
    - from: "CLAUDE.md task routing"
      to: ".claude/skills/visual-orchestrator/SKILL.md"
      via: "Create shot list row points to visual-orchestrator"
      pattern: "Create shot list.*visual-orchestrator"
    - from: "SKILL.md Step 2"
      to: ".claude/skills/visual-orchestrator/prompts/generation.md"
      via: "Read the generation prompt instruction"
      pattern: "generation\\.md"
    - from: "SKILL.md Step 1"
      to: "projects/N. [Title]/Script.md"
      via: "Project resolution and Script.md read"
      pattern: "Script\\.md"
---

<objective>
Write SKILL.md for the visual-orchestrator skill and update CLAUDE.md with routing entries, making the skill discoverable and invocable.

Purpose: Close the last gap in v1.3 — the skill's CONTEXT.md (Phase 13) and generation.md (Phase 14) exist but nothing tells users how to invoke it or routes them to it.
Output: SKILL.md at `.claude/skills/visual-orchestrator/SKILL.md`, updated CLAUDE.md with 4 targeted edits.
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-skill-entry-point/15-CONTEXT.md
@.planning/phases/15-skill-entry-point/15-RESEARCH.md

@.claude/skills/writer/SKILL.md
@.claude/skills/visual-orchestrator/CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Template: writer/SKILL.md structure (frontmatter, tagline, Workflow, Prerequisites, Output) -->
<!-- Contract: visual-orchestrator/CONTEXT.md defines inputs, process, outputs, pipeline-reset invariant -->
<!-- Target: CLAUDE.md lines 63, 74, 37-47, 87-92 contain the 4 edit locations -->

From writer/SKILL.md (structural template):
```
--- frontmatter (name, description with trigger phrases) ---
# [Skill Name]
[One-liner tagline]
## Workflow
1. [Step 1: resolve project]
2. [Step 2: read prompt]
3. [Step 3: generate + write]
## Prerequisites
- [upstream outputs]
## Output
[path, format, re-run behavior]
```

CLAUDE.md edit targets:
- Line 63: `| Create shot list | *(not yet implemented)* | — |`
- Line 74: `| Visual planning *(future)* | visual-references/*/VISUAL_STYLE_GUIDE.md, projects/N/script.md | competitors/, channel.md — director needs visuals + script |`
- Folder Map (lines 37-47): missing visual-orchestrator under skills/
- Pipeline Skills table (lines 87-92): missing visual-orchestrator row
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md for visual-orchestrator</name>
  <files>.claude/skills/visual-orchestrator/SKILL.md</files>
  <action>
Create `.claude/skills/visual-orchestrator/SKILL.md` modeled directly on `writer/SKILL.md` structure. The file should be approximately 30-40 lines (similar to writer's 33 lines — NOT the 108-line style-extraction).

**Frontmatter** (use exact text from 15-RESEARCH.md Code Examples):
```yaml
---
name: visual-orchestrator
description: Generate structured shot lists from finished documentary scripts. Use this skill when the user wants to create a shot list, generate a shotlist, parse a script into shots, or says things like "create shot list", "generate shotlist", "parse script into shots", "visual plan for [topic]", "shotlist for [topic]". This is a [HEURISTIC] skill — zero Python, Claude does all reasoning. Requires a completed Script.md in the project directory.
---
```

**Body structure:**
1. H1 heading: `# Visual Orchestrator`
2. One-liner tagline: `Script → structured shot list for the asset pipeline.`
3. `## Workflow` with 3 numbered steps:
   - **Step 1: Resolve project directory** — Topic is a case-insensitive substring match against `projects/` directory names. Multiple matches: list all, ask user which one. No match: error with guidance to check spelling or use `cmd_init_project`. Found: read `projects/N. [Title]/Script.md`. If Script.md is missing: stop and tell user to run the writer skill first (include the trigger phrase "write the script for [topic]").
   - **Step 2: Read the generation prompt** — `.claude/skills/visual-orchestrator/prompts/generation.md`
   - **Step 3: Generate the shot list** — Follow the prompt instructions. Write output to `projects/N. [Title]/shotlist.json`, overwriting any existing file without prompt.
4. `## Prerequisites` listing:
   - `projects/N. [Title]/Script.md` — from the writer skill
   - `.claude/skills/visual-orchestrator/prompts/generation.md` — shot generation rules
5. `## Output` — one-liner: `projects/N. [Title]/shotlist.json` — JSON array of shot objects. Full overwrite on re-run; previous versions preserved in git history.

**Critical anti-patterns to avoid:**
- Do NOT mention VISUAL_STYLE_GUIDE.md anywhere — completely skipped in v1.3 per user decision
- Do NOT add a fallback to `.claude/scratch/` — visual-orchestrator requires a proper project directory per user decision
- Do NOT bloat with long explanations — keep concise like writer/SKILL.md
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && grep -q 'visual-orchestrator' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && grep -q 'HEURISTIC' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && grep -q 'Script.md' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && grep -q 'generation.md' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && grep -q 'shotlist.json' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/SKILL.md' && echo PASS || echo FAIL"</automated>
  </verify>
  <done>SKILL.md exists at `.claude/skills/visual-orchestrator/SKILL.md` with YAML frontmatter containing trigger phrases, 3-step workflow (resolve project, read prompt, generate+write), Prerequisites section, and Output section. No mention of VISUAL_STYLE_GUIDE. No .claude/scratch/ fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with visual-orchestrator routing</name>
  <files>CLAUDE.md</files>
  <action>
Make exactly 4 surgical edits to `CLAUDE.md`. Read the file first, then apply each edit. Do NOT rewrite the entire file — edit only the targeted lines.

**Edit 1 — Task Routing table (line 63):**
Replace:
```
| Create shot list | *(not yet implemented)* | — |
```
With:
```
| Create shot list | visual-orchestrator | SKILL.md invocation |
```

**Edit 2 — What to Load table (line 74):**
Replace:
```
| Visual planning *(future)* | visual-references/*/VISUAL_STYLE_GUIDE.md, projects/N/script.md | competitors/, channel.md — director needs visuals + script |
```
With:
```
| Visual planning | visual-orchestrator/CONTEXT.md, .claude/skills/visual-orchestrator/prompts/generation.md, projects/N/Script.md | visual-references/ (deferred to SHOT-09), competitors/, channel.md — director needs script + generation rules only |
```
Note: `Script.md` with capital S (not lowercase `script.md`). List `CONTEXT.md` (not `SKILL.md`) in the Load column per the Stage Contracts convention.

**Edit 3 — Folder Map (around line 41, after the writer/ entry):**
Add this line after `│   │   ├── writer/`:
```
│   │   ├── visual-orchestrator/      # Agent 1.4: Script → structured shot list
```

**Edit 4 — Pipeline Skills table (after the visual-style-extractor row, around line 92):**
Add a new row:
```
| `visual-orchestrator` | 1.4 | Script → structured shot list |
```

**Verify after editing:** The Stage Contracts convention note (line ~107-108) should still read correctly without changes — confirm it does and leave it unchanged.

**Anti-patterns:**
- Do NOT change the Stage Contracts convention note
- Do NOT add visual-orchestrator to the Utility Skills table (it is a pipeline skill)
- Do NOT modify any other rows in the routing tables
  </action>
  <verify>
    <automated>bash -c "grep -q 'visual-orchestrator.*SKILL.md invocation' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/CLAUDE.md' && grep -q 'Visual planning.*CONTEXT.md' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/CLAUDE.md' && grep -q 'visual-orchestrator.*1.4' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/CLAUDE.md' && grep -q 'visual-orchestrator/.*Agent 1.4' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/CLAUDE.md' && echo PASS || echo FAIL"</automated>
  </verify>
  <done>CLAUDE.md contains all 4 updates: (1) Task Routing row points to visual-orchestrator, (2) What to Load row lists CONTEXT.md + generation.md + Script.md with correct casing, (3) Folder Map includes visual-orchestrator entry, (4) Pipeline Skills table includes Agent 1.4 row. Stage Contracts note unchanged.</done>
</task>

</tasks>

<verification>
1. SKILL.md exists at `.claude/skills/visual-orchestrator/SKILL.md` and contains the 3-step workflow
2. CLAUDE.md Task Routing table no longer contains "not yet implemented" for shot list
3. CLAUDE.md What to Load table no longer contains "*(future)*" for visual planning
4. CLAUDE.md Folder Map includes visual-orchestrator
5. CLAUDE.md Pipeline Skills table includes visual-orchestrator as Agent 1.4
6. No mention of VISUAL_STYLE_GUIDE in SKILL.md
7. Stage Contracts convention note in CLAUDE.md is unchanged
</verification>

<success_criteria>
- SKILL.md is discoverable (frontmatter trigger phrases match common invocations) and invocable (3-step workflow is unambiguous)
- CLAUDE.md routes "Create shot list" tasks to visual-orchestrator
- All four CLAUDE.md edit locations are updated, no other sections modified
- INFRA-03 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/15-skill-entry-point/15-01-SUMMARY.md`
</output>
