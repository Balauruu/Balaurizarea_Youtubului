You are analyzing frames from a documentary video to extract its visual style.
Each frame is labeled with an ID, timestamp, and narration snippet.

For EACH frame in this contact sheet, output a JSON object with these fields:

1. **frame_id**: The frame label (e.g., "F001")

2. **scene_type**: Classify into one of these categories (or propose "other: ___"):
   [title_card | archival_photo | archival_video | news_clip | map_animation |
    text_overlay | quote_card | b_roll_footage | screen_recording |
    animated_graphic | silhouette | date_card | chapter_header |
    evidence_document | portrait | location_shot | other: ___]

3. **shotlist_type**: Map to the closest shotlist category:
   [archival_photo | archival_video | news_clip | animation | ai_generated | map | text_overlay | document_scan]
   Use your best judgment. For example: silhouette → ai_generated, chapter_header → text_overlay, date_card → animation.

4. **asset_breakdown**:
   - background: (solid color, gradient, footage, etc.)
   - main_element: (photo, video, graphic, text, etc.)
   - overlays: (grain, scanlines, vignette, color filter, etc. — or "none")
   - text_elements: (describe font style, position, color — or "none")

5. **dominant_colors**: List 2-3 dominant colors as descriptive names (e.g., "deep red", "black", "warm orange").

6. **content_description**: Describe WHAT is depicted in the frame.
   Capture the literal visual content.
   (e.g., "black-and-white archival footage of silhouetted figures near palm trees",
    "bold red text reading 'CRUEL' with glitch distortion on black background")

7. **narrative_trigger**: Answer the question: "In general, when would a documentary use this type of visual?"
   Do NOT reference the specific story or script. Write a general rule.
   (e.g., "Used to present direct testimony or damning primary-source quotes without visual distraction",
    "Used for geographic orientation when introducing a new location in the narrative",
    "Used as a pacing device — a beat of darkness between high-density information segments")

8. **confidence**: Rate 1-5 how clearly you can analyze this frame.
   If below 3, note what is unclear.

Output as a JSON array. Only describe what you can see.
If you cannot determine something, say "unclear" — do not guess.
