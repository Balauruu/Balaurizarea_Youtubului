---
phase: 13-stage-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/visual-orchestrator/CONTEXT.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
must_haves:
  truths:
    - "CONTEXT.md exists at .claude/skills/visual-orchestrator/CONTEXT.md"
    - "Skill is classified as pure [HEURISTIC] with zero Python code"
    - "Inputs list exactly two sources: Script.md and prompts/generation.md — NOT VISUAL_STYLE_GUIDE.md"
    - "Pipeline-reset invariant is documented as a design principle with generic downstream references"
    - "No checkpoint section exists in the file"
    - "Deferred section lists both out-of-scope items and future enhancements"
  artifacts:
    - path: ".claude/skills/visual-orchestrator/CONTEXT.md"
      provides: "Visual orchestrator stage contract"
      contains: "[HEURISTIC]"
  key_links:
    - from: ".claude/skills/visual-orchestrator/CONTEXT.md"
      to: "prompts/generation.md"
      via: "Inputs table reference"
      pattern: "prompts/generation\\.md"
    - from: ".claude/skills/visual-orchestrator/CONTEXT.md"
      to: "projects/N. [Title]/Script.md"
      via: "Inputs table reference"
      pattern: "Script\\.md"
---

<objective>
Write the stage contract (CONTEXT.md) for the visual-orchestrator skill, locking its inputs, outputs, process steps, pipeline-reset invariant, and deferred scope.

Purpose: This contract defines the skill boundary so Phase 14 (generation prompt) and Phase 15 (SKILL.md) can be built against a stable specification. It also serves as the orchestrator routing reference from CLAUDE.md.

Output: `.claude/skills/visual-orchestrator/CONTEXT.md`
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-stage-contract/13-CONTEXT.md
@.planning/phases/13-stage-contract/13-RESEARCH.md

@.claude/skills/style-extraction/CONTEXT.md
@.claude/skills/writer/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create visual-orchestrator directory and write CONTEXT.md</name>
  <files>.claude/skills/visual-orchestrator/CONTEXT.md</files>
  <action>
Create the directory `.claude/skills/visual-orchestrator/` if it does not exist, then write `CONTEXT.md` following the style-extraction/CONTEXT.md template structure with these exact sections in order:

**Title line:** `# Visual Orchestrator — Stage Contract` followed by a one-line description: parses finished scripts into structured shot lists for the asset pipeline.

**## Inputs** — 4-column table (Source | File/Location | Scope | Why) with exactly TWO rows:
1. Writer skill | `projects/N. [Title]/Script.md` | Full file | Source material for chapter parsing and shot generation
2. Skill prompt | `.claude/skills/visual-orchestrator/prompts/generation.md` | Full file | Schema definition, granularity rules, building block vocabulary, anti-patterns

Do NOT include VISUAL_STYLE_GUIDE.md — it is deferred to SHOT-09.

**## Process** — Numbered steps, each prefixed with `[HEURISTIC]`:
1. Read inputs — Load Script.md and prompts/generation.md
2. Parse chapter structure — Identify chapters by `## N. Title` headings; treat unnumbered prologue as chapter 0
3. Generate shots — For each chapter, identify narrative beats and assign shots following prompts/generation.md rules
4. Write shotlist.json — Write to `projects/N. [Title]/shotlist.json`, overwriting any existing file without prompt

Exact wording of step descriptions is at your discretion but must convey the above content.

**## Pipeline-Reset Invariant** — A standalone prose section (NOT a checkpoint table) expressing all five points:
1. shotlist.json is the source of truth for the asset pipeline
2. Any Script.md change requires full regeneration
3. Downstream artifacts mentioned generically (e.g., "acquisition manifests") — do NOT name manifest.json
4. No partial or chapter-level regeneration in v1.3
5. Reason: sequential shot IDs (S001...) are assigned globally; chapter-level regen would fragment the ID sequence

Include that git history preserves previous versions, and re-invoking the skill regenerates in full.

**## Outputs** — 3-column table (Artifact | Location | Format) with ONE row:
- Shot list | `projects/N. [Title]/shotlist.json` | JSON array of shot objects

Do NOT include field-level schema detail — that belongs in prompts/generation.md (Phase 14).

**## Deferred** — Two subsections:

*Out of scope by design:*
- Shot duration/timing — editor's domain (DaVinci Resolve)
- Camera angles/movement — not an acquisition pipeline, only sourcing
- Effects/transitions — post-production decisions
- Priority/importance scoring — acquisition gets all shots regardless

*Future enhancements (planned):*
- SHOT-09: VISUAL_STYLE_GUIDE.md as input for building block vocabulary
- SHOT-08: Two-pass generation (annotate beats first, then assign types)
- SHOT-10: Shot density calibration based on chapter word count
- Chapter-level regeneration without full re-run

**## Classification** — Final line exactly: `**[HEURISTIC]** — Zero Python. No CLI commands. No pip installs.`

CRITICAL constraints:
- Do NOT include a ## Checkpoints section (locked decision: no checkpoint)
- Do NOT include a ## Post-Write Actions section (not needed)
- Do NOT list shotlist.json field names — schema detail belongs in Phase 14
- Do NOT mention manifest.json by name in the invariant section
  </action>
  <verify>
    <automated>bash -c "test -f 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/CONTEXT.md' && grep -q 'HEURISTIC' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/CONTEXT.md' && grep -q 'Pipeline-Reset' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/CONTEXT.md' && grep -q 'generation.md' 'D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/CONTEXT.md' && echo PASS || echo FAIL"</automated>
  </verify>
  <done>
    - CONTEXT.md exists at .claude/skills/visual-orchestrator/CONTEXT.md
    - Contains exactly 6 sections: title, Inputs, Process, Pipeline-Reset Invariant, Outputs, Deferred, Classification
    - Inputs table has exactly 2 rows (Script.md and generation.md) — no VISUAL_STYLE_GUIDE.md
    - No Checkpoints section present
    - Pipeline-reset invariant mentions downstream artifacts generically, not manifest.json by name
    - Classification line reads [HEURISTIC] with zero Python
    - Deferred section has both out-of-scope and future enhancement categories
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate CONTEXT.md against Phase 13 success criteria</name>
  <files>.claude/skills/visual-orchestrator/CONTEXT.md</files>
  <action>
Read the written CONTEXT.md and verify it against all four ROADMAP success criteria for Phase 13:

1. Classifies skill as pure [HEURISTIC] with zero Python code
2. Documents inputs (Script.md, prompts/generation.md), numbered process steps, and outputs (shotlist.json path)
3. Explicitly documents the pipeline-reset invariant: shotlist.json invalidation on Script.md change requires full regeneration
4. Lists deferred items (shot duration, timing, camera movement, effects, transitions)

Also verify the anti-patterns are avoided:
- No VISUAL_STYLE_GUIDE.md in inputs (Pitfall 1)
- No Checkpoints section (Pitfall 2)
- No manifest.json named in invariant (Pitfall 3)
- No schema field names in CONTEXT.md (Pitfall 4)
- Deferred section is present and specific (Pitfall 5)

If any check fails, fix the file immediately.
  </action>
  <verify>
    <automated>bash -c "FILE='D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3/.claude/skills/visual-orchestrator/CONTEXT.md' && PASS=true && (grep -q 'HEURISTIC.*Zero Python' \"$FILE\" || { echo 'FAIL: Missing classification'; PASS=false; }) && (grep -q 'Script.md' \"$FILE\" || { echo 'FAIL: Missing Script.md input'; PASS=false; }) && (grep -q 'generation.md' \"$FILE\" || { echo 'FAIL: Missing generation.md input'; PASS=false; }) && (grep -q 'Pipeline-Reset' \"$FILE\" || { echo 'FAIL: Missing invariant'; PASS=false; }) && (grep -q 'Deferred' \"$FILE\" || { echo 'FAIL: Missing deferred'; PASS=false; }) && (! grep -q 'Checkpoint' \"$FILE\" || { echo 'FAIL: Has checkpoint section'; PASS=false; }) && (! grep -q 'manifest.json' \"$FILE\" || { echo 'FAIL: Names manifest.json'; PASS=false; }) && ($PASS && echo 'ALL CHECKS PASS')"</automated>
  </verify>
  <done>
    - All 4 ROADMAP success criteria confirmed present
    - All 5 anti-patterns confirmed absent
    - File is ready for Phase 14 to reference as the locked contract
  </done>
</task>

</tasks>

<verification>
1. File exists at `.claude/skills/visual-orchestrator/CONTEXT.md`
2. Section order matches: title, Inputs, Process, Pipeline-Reset Invariant, Outputs, Deferred, Classification
3. Two inputs only (Script.md, generation.md)
4. Four numbered [HEURISTIC] process steps
5. Pipeline-reset invariant present with generic downstream references
6. One output row (shotlist.json)
7. Deferred section with two categories (out-of-scope + future)
8. Classification line present
9. No Checkpoints section
10. No manifest.json named
11. No schema field names
</verification>

<success_criteria>
- CONTEXT.md is a complete, self-contained stage contract that an orchestrator can read to understand what the visual-orchestrator skill does, what it needs, and what it produces
- All ROADMAP Phase 13 success criteria are met
- The file follows the established style-extraction/CONTEXT.md template pattern
- Phase 14 can begin work on generation.md using this contract as its specification
</success_criteria>

<output>
After completion, create `.planning/phases/13-stage-contract/13-01-SUMMARY.md`
</output>
