---
phase: 07-scraping-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/researcher/SKILL.md
  - .claude/skills/researcher/scripts/researcher/__init__.py
  - .claude/skills/researcher/scripts/researcher/__main__.py
  - .claude/skills/researcher/scripts/researcher/tiers.py
  - .claude/skills/researcher/scripts/researcher/fetcher.py
  - tests/test_researcher/__init__.py
  - tests/test_researcher/test_tiers.py
  - tests/test_researcher/test_fetcher.py
  - pytest.ini
autonomous: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03

must_haves:
  truths:
    - "fetcher.py creates a fresh AsyncWebCrawler context per fetch call — no session_id reuse across domains"
    - "A fetch returning fewer than 200 characters triggers retry and is logged as failed, not silently dropped"
    - "Tier 3 URLs are skipped before any fetch attempt and logged as skipped_tier3"
    - "classify_domain() returns 1 for known Tier 1 domains, 3 for known Tier 3 domains, and 2 for unknown domains"
  artifacts:
    - path: ".claude/skills/researcher/scripts/researcher/fetcher.py"
      provides: "crawl4ai wrapper with domain isolation, retry, content validation"
      exports: ["fetch_url", "fetch_with_retry"]
    - path: ".claude/skills/researcher/scripts/researcher/tiers.py"
      provides: "Source tier constants and classify_domain()"
      exports: ["TIER_1_DOMAINS", "TIER_2_DOMAINS", "TIER_3_DOMAINS", "TIER_RETRY_MAP", "classify_domain"]
    - path: ".claude/skills/researcher/SKILL.md"
      provides: "Skill index for researcher"
    - path: "tests/test_researcher/test_fetcher.py"
      provides: "Unit tests for fetcher with mocked crawl4ai"
    - path: "tests/test_researcher/test_tiers.py"
      provides: "Unit tests for tier classification"
  key_links:
    - from: ".claude/skills/researcher/scripts/researcher/fetcher.py"
      to: ".claude/skills/researcher/scripts/researcher/tiers.py"
      via: "imports classify_domain and TIER_RETRY_MAP"
      pattern: "from researcher\\.tiers import"
---

<objective>
Create the researcher skill scaffold and implement the core scraping layer: fetcher.py (crawl4ai wrapper with domain-isolated browser contexts, retry logic, minimum content validation) and tiers.py (source tier constants with domain classification).

Purpose: This is the foundation that all downstream phases (8-10) depend on. Every web fetch in the research pipeline goes through fetcher.py, and every URL is classified by tiers.py before fetching.

Output: Working fetcher.py and tiers.py modules with passing unit tests. Skill directory structure created.
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scraping-foundation/07-CONTEXT.md
@.planning/phases/07-scraping-foundation/07-RESEARCH.md

<interfaces>
<!-- crawl4ai 0.8 API (from research) -->
```python
from crawl4ai import AsyncWebCrawler, BrowserConfig, CrawlerRunConfig, CacheMode

# BrowserConfig: use_persistent_context=False, headless=True, browser_type="chromium"
# CrawlerRunConfig: cache_mode=CacheMode.BYPASS
# result.success: bool
# result.markdown.raw_markdown: str (NOT result.markdown as string)
# result.error_message: str
```

<!-- Existing retry pattern from channel-assistant scraper.py -->
```python
# _run_ytdlp() pattern: max_attempts=3, delay * (attempt + 1) progressive
```

<!-- pytest.ini (current) -->
```ini
[pytest]
pythonpath = .claude/skills/channel-assistant/scripts
testpaths = tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create researcher skill scaffold + tiers.py with tests</name>
  <files>
    .claude/skills/researcher/SKILL.md
    .claude/skills/researcher/scripts/researcher/__init__.py
    .claude/skills/researcher/scripts/researcher/__main__.py
    .claude/skills/researcher/scripts/researcher/tiers.py
    .claude/skills/researcher/prompts/.gitkeep
    tests/test_researcher/__init__.py
    tests/test_researcher/test_tiers.py
    pytest.ini
  </files>
  <behavior>
    - classify_domain("https://en.wikipedia.org/wiki/Test") returns 1
    - classify_domain("https://archive.org/details/test") returns 1
    - classify_domain("https://www.facebook.com/page") returns 3
    - classify_domain("https://x.com/user") returns 3
    - classify_domain("https://reddit.com/r/test") returns 3
    - classify_domain("https://unknowndomain.example.com/page") returns 2
    - classify_domain("https://www.en.wikipedia.org/wiki/Test") returns 1 (strips www.)
    - TIER_RETRY_MAP[1] == 3, TIER_RETRY_MAP[2] == 1, TIER_RETRY_MAP[3] == 0
  </behavior>
  <action>
    1. Create directory structure: `.claude/skills/researcher/scripts/researcher/` and `.claude/skills/researcher/prompts/`
    2. Create `SKILL.md` — minimal index referencing the researcher skill, CLI invocation pattern (`PYTHONPATH=.claude/skills/researcher/scripts python -m researcher <subcommand>`), and listing modules (fetcher.py, tiers.py, url_builder.py, cli.py). Mark Phase 7 modules and note Phase 8-10 modules as planned.
    3. Create `__init__.py` (empty) and `__main__.py` (imports and calls cli.main(), with placeholder that prints "researcher: no subcommand specified" if cli.py doesn't exist yet).
    4. Create `tiers.py` following the exact pattern from 07-RESEARCH.md Pattern 3:
       - TIER_1_DOMAINS frozenset: archive.org, loc.gov, archives.gov, commons.wikimedia.org, en.wikipedia.org, images.nasa.gov, nps.gov, nationalarchives.gov.uk, publicdomainreview.org
       - TIER_2_DOMAINS frozenset: britannica.com, bbc.com, theguardian.com, nytimes.com, reuters.com, apnews.com, html.duckduckgo.com, duckduckgo.com
       - TIER_3_DOMAINS frozenset: facebook.com, twitter.com, x.com, instagram.com, tiktok.com, reddit.com, pinterest.com
       - TIER_RETRY_MAP: {1: 3, 2: 1, 3: 0}
       - classify_domain(url: str) -> int: uses urlparse().hostname, strips "www." prefix, returns 1/2/3
       - Domain-only matching (Claude's discretion: domain-only is simpler and sufficient for tier classification)
    5. Update `pytest.ini` to add researcher scripts path: `pythonpath = .claude/skills/channel-assistant/scripts .claude/skills/researcher/scripts`
    6. Create `tests/test_researcher/__init__.py` (empty)
    7. Create `tests/test_researcher/test_tiers.py` with tests for all behaviors listed above. Use plain pytest assertions.
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=".claude/skills/researcher/scripts" pytest tests/test_researcher/test_tiers.py -x --tb=short</automated>
  </verify>
  <done>
    - Researcher skill directory exists with SKILL.md, __init__.py, __main__.py, tiers.py
    - tiers.py exports TIER_1_DOMAINS, TIER_2_DOMAINS, TIER_3_DOMAINS, TIER_RETRY_MAP, classify_domain
    - All test_tiers.py tests pass
    - pytest.ini updated with researcher scripts path
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fetcher.py with domain isolation, retry, and content validation</name>
  <files>
    .claude/skills/researcher/scripts/researcher/fetcher.py
    tests/test_researcher/test_fetcher.py
  </files>
  <behavior>
    - fetch_with_retry with a mocked successful crawl4ai result (>200 chars) returns {success: True, content: ..., attempts_used: 1}
    - fetch_with_retry with content < 200 chars retries up to max_attempts then returns {success: False, error: "content too short (N chars)"}
    - fetch_with_retry with result.success=False retries up to max_attempts then returns {success: False, error: ...}
    - fetch_with_retry creates a NEW AsyncWebCrawler context per call (domain isolation) — verify BrowserConfig(use_persistent_context=False) and CacheMode.BYPASS are used
    - fetch_with_retry for a Tier 3 URL returns {success: False, fetch_status: "skipped_tier3"} without calling crawl4ai
    - fetch_with_retry logs warnings on retry attempts
  </behavior>
  <action>
    1. Create `tests/test_researcher/test_fetcher.py` FIRST (TDD red phase):
       - Mock `crawl4ai.AsyncWebCrawler` using unittest.mock.patch or a mock factory
       - test_domain_isolation: Verify that _fetch_once creates AsyncWebCrawler with BrowserConfig(use_persistent_context=False) and CrawlerRunConfig(cache_mode=CacheMode.BYPASS). Mock the crawler context manager.
       - test_content_length_validation: Mock a successful result with 50 chars of content. Verify fetch_with_retry returns success=False with error containing "content too short".
       - test_retry_exhaustion: Mock result.success=False for all attempts. Verify fetch_with_retry returns success=False and attempts_used equals max_attempts.
       - test_successful_fetch: Mock result.success=True with 500 chars of content. Verify returns success=True, content is the text, attempts_used=1.
       - test_tier3_skipped: Call fetch_with_retry with a Tier 3 URL (e.g., facebook.com). Verify it returns immediately with fetch_status="skipped_tier3" and crawl4ai is never called.
    2. Run tests — confirm they FAIL (red).
    3. Create `fetcher.py` following 07-RESEARCH.md Pattern 1 and Pattern 2:
       - `_browser_conf = BrowserConfig(browser_type="chromium", headless=True, use_persistent_context=False, verbose=False)`
       - `async def _fetch_once(url: str) -> tuple[bool, str, str]`: Creates CrawlerRunConfig(cache_mode=CacheMode.BYPASS), opens `async with AsyncWebCrawler(config=_browser_conf) as crawler`, calls `await crawler.arun(url=url, config=run_conf)`. Returns (success, result.markdown.raw_markdown, error_message).
       - `def fetch_url(url: str) -> tuple[bool, str, str]`: Sync wrapper using `asyncio.run(_fetch_once(url))`.
       - `MIN_CONTENT_CHARS = 200` (SCRP-02 threshold, start at 200 per user decision)
       - `def fetch_with_retry(url: str, max_attempts: int = 3, delay_seconds: float = 5.0) -> dict`:
         - First checks tier via `classify_domain(url)`. If tier 3, return immediately with `{"success": False, "content": "", "error": "tier 3 domain skipped", "fetch_status": "skipped_tier3", "attempts_used": 0}`.
         - Otherwise use TIER_RETRY_MAP to determine max_attempts override (tier 1: 3 retries, tier 2: 1 retry).
         - Retry loop: call fetch_url, check success AND len(text) >= MIN_CONTENT_CHARS. Log warnings on failure. Progressive delay: `time.sleep(delay_seconds * attempt)`.
         - Return dict: `{success, content, error, fetch_status, attempts_used}` where fetch_status is "ok", "failed", or "skipped_tier3".
       - Uses `logging.getLogger(__name__)` for all log output.
       - Import classify_domain and TIER_RETRY_MAP from researcher.tiers.
    4. Run tests — confirm they PASS (green).

    IMPORTANT: Do NOT install crawl4ai in this task. Tests mock crawl4ai entirely. Installation happens at execution time when the executor runs `pip install crawl4ai==0.8.0 && crawl4ai-setup` as a prerequisite (documented in SKILL.md from Task 1).
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=".claude/skills/researcher/scripts" pytest tests/test_researcher/test_fetcher.py -x --tb=short</automated>
  </verify>
  <done>
    - fetcher.py exports fetch_url, fetch_with_retry
    - fetch_with_retry uses domain-isolated browser contexts (fresh AsyncWebCrawler per call, use_persistent_context=False, CacheMode.BYPASS)
    - Content < 200 chars triggers retry and logs warning
    - Tier 3 URLs skipped without fetching
    - All test_fetcher.py tests pass with mocked crawl4ai
  </done>
</task>

</tasks>

<verification>
```bash
cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3"
PYTHONPATH=".claude/skills/researcher/scripts" pytest tests/test_researcher/ -x --tb=short
```
All tests in test_tiers.py and test_fetcher.py pass.
</verification>

<success_criteria>
- Researcher skill directory exists at `.claude/skills/researcher/` with SKILL.md and module structure
- tiers.py correctly classifies domains into 3 tiers with Tier 2 as default for unknowns
- fetcher.py wraps crawl4ai with domain-isolated contexts, retry logic, and content length validation
- Tier 3 URLs never trigger a crawl4ai fetch
- All unit tests pass (mocked crawl4ai — no browser needed for tests)
- pytest.ini includes researcher scripts path
</success_criteria>

<output>
After completion, create `.planning/phases/07-scraping-foundation/07-01-SUMMARY.md`
</output>
