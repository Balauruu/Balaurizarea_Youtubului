---
phase: 02-query-layer-competitor-analysis
plan: 03
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified:
  - pytest.ini
  - context/competitors/analysis.md
autonomous: true
gap_closure: true
requirements: [DATA-05, ANLZ-01, ANLZ-02, ANLZ-03]
must_haves:
  truths:
    - "analysis.md Topic Clusters section contains actual topic groupings with saturation levels, not a placeholder comment"
    - "analysis.md Title Patterns section contains structural formulas and performance correlations, not a placeholder comment"
    - "Running 'python -m pytest tests/test_channel_assistant/' without manually setting PYTHONPATH succeeds"
  artifacts:
    - path: "pytest.ini"
      provides: "PYTHONPATH configuration for test discovery"
      contains: "pythonpath"
    - path: "context/competitors/analysis.md"
      provides: "Complete competitor analysis with all 4 sections populated"
      min_lines: 40
  key_links:
    - from: "context/competitors/analysis.md"
      to: ".claude/scratch/video_data_for_analysis.md"
      via: "Claude reads video data and writes heuristic analysis"
      pattern: "Topic Clusters|Title Patterns"
---

<objective>
Close 3 verification gaps from Phase 2: (1) add pytest.ini so tests run without manual PYTHONPATH, (2) populate Topic Clusters section of analysis.md with actual topic groupings and saturation assessments by reasoning over video_data_for_analysis.md, (3) populate Title Patterns section with structural formulas and performance correlations.

Purpose: Phase 2 deterministic code is complete, but the [HEURISTIC] analysis step was never executed. Phase 3 reads analysis.md as input context -- empty sections mean no cluster or pattern data for topic generation.
Output: Complete analysis.md with all 4 sections populated; pytest.ini for ergonomic test runs.
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-query-layer-competitor-analysis/02-02-SUMMARY.md
@context/competitors/analysis.md
@.claude/scratch/video_data_for_analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest.ini with PYTHONPATH configuration</name>
  <files>pytest.ini</files>
  <action>
Create pytest.ini in the project root with the following content:

```ini
[pytest]
pythonpath = .claude/skills/channel-assistant/scripts
testpaths = tests
```

This ensures `from channel_assistant.models import ...` resolves without manually setting PYTHONPATH. The `testpaths` directive tells pytest where to find tests.
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && python -m pytest tests/test_channel_assistant/test_analyzer.py -x -q 2>&1 | head -5</automated>
  </verify>
  <done>All 17 analyzer tests pass when running bare `python -m pytest` without PYTHONPATH environment variable</done>
</task>

<task type="auto">
  <name>Task 2: Execute heuristic analysis -- populate Topic Clusters and Title Patterns in analysis.md</name>
  <files>context/competitors/analysis.md</files>
  <action>
This is a [HEURISTIC] task per Architecture.md. Do NOT write Python code. Instead:

1. Read `.claude/scratch/video_data_for_analysis.md` (already loaded in context above).
2. Read the current `context/competitors/analysis.md` to preserve the existing Channel Stats and Outlier Videos sections exactly as they are.
3. Reason over the video data to identify topic clusters and title patterns.
4. Replace the `<!-- HEURISTIC: ... -->` placeholder in the `## Topic Clusters` section with actual content following these rules from CONTEXT.md:
   - Two-level hierarchy: Broad theme > specific topics
   - Saturation assessment per cluster using three factors: coverage count (how many channels), recency (when last covered), performance (views relative to median)
   - Three saturation levels: Oversaturated, Moderate, Underserved
   - Editorial recommendation per cluster (e.g., "proven demand, needs unique angle" or "fresh territory")
5. Replace the `<!-- HEURISTIC: ... -->` placeholder in the `## Title Patterns` section with actual content following these rules:
   - Extract structural formulas (e.g., "The [Noun] of [Location/Entity]", "Question + Entity")
   - Extract emotional hooks (mystery/suspense, revelation, warning)
   - Rank patterns by performance -- which formulas correlate with higher views
   - Flag sample size when pattern has fewer than 5 examples
   - Show concrete examples from the data for each pattern
6. Write the complete updated analysis.md, preserving the header, generation timestamp, Channel Stats table, and Outlier Videos list verbatim. Only replace the two placeholder sections.

Key data points to reason over (37 Barely Sociable videos + 3 Unnamed TV videos):
- Barely Sociable median: 2,262,202 views; covers dark web, internet mysteries, unsolved disappearances, aviation mysteries, Reddit mysteries, crypto
- Unnamed TV median: 26,571 views; covers cults, political assassination, unsolved cases
- Outliers: Silk Road (13.1M, dark web), Dream/Dark Net (4.7M, dark web), Mexico cult (2.2M, cults/true crime)
- Tags are mostly empty for Barely Sociable; Unnamed TV has tags like "cult", "true crime", "mystery", "unsolved"
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && python -c "content = open('context/competitors/analysis.md', encoding='utf-8').read(); assert 'HEURISTIC' not in content, 'Still has placeholder'; assert '## Topic Clusters' in content, 'Missing section'; assert '## Title Patterns' in content, 'Missing section'; lines = content.split('\n'); tc_idx = next(i for i,l in enumerate(lines) if '## Topic Clusters' in l); tp_idx = next(i for i,l in enumerate(lines) if '## Title Patterns' in l); assert tp_idx - tc_idx > 5, f'Topic Clusters too short: {tp_idx - tc_idx} lines'; assert len(lines) - tp_idx > 5, f'Title Patterns too short'; print('All checks passed')"</automated>
  </verify>
  <done>analysis.md contains populated Topic Clusters with two-level hierarchy and saturation assessments, and Title Patterns with structural formulas ranked by performance. No placeholder comments remain. File is a self-contained briefing document ready for Phase 3 consumption.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_channel_assistant/ -x -q` passes all 17 tests without PYTHONPATH
2. `context/competitors/analysis.md` has no HTML comment placeholders
3. Topic Clusters section has at least 3 cluster groupings with saturation levels
4. Title Patterns section has at least 3 structural formulas with performance data
5. Channel Stats and Outlier Videos sections unchanged from original
</verification>

<success_criteria>
- All 3 verification gaps from 02-VERIFICATION.md are closed
- analysis.md is a complete, self-contained competitor briefing with all 4 sections populated
- Tests run without manual PYTHONPATH configuration
- Phase 2 requirements DATA-05, ANLZ-01, ANLZ-02, ANLZ-03 are fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-query-layer-competitor-analysis/02-03-SUMMARY.md`
</output>
