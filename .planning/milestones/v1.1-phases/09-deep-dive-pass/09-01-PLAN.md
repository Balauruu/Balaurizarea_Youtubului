---
phase: 09-deep-dive-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/researcher/scripts/researcher/cli.py
  - .claude/skills/researcher/SKILL.md
  - tests/test_researcher/test_cli.py
autonomous: true
requirements: [RSRCH-03]

must_haves:
  truths:
    - "Running cmd_deepen reads deep_dive_urls only from sources with verdict 'recommended'"
    - "Pass 2 fetched content is written as pass2_NNN.json files distinct from Pass 1 src_NNN.json files"
    - "Total source files across both passes never exceeds 15"
    - "URLs already fetched in Pass 1 are not re-fetched in Pass 2"
    - "Tier 3 URLs in deep_dive_urls are skipped"
    - "When no deep_dive_urls exist after filtering, cmd_deepen exits cleanly with a message"
    - "source_manifest.json is updated with a pass2_sources key after Pass 2 completes"
  artifacts:
    - path: ".claude/skills/researcher/scripts/researcher/cli.py"
      provides: "cmd_deepen function, _collect_deep_dive_urls helper, _get_fetched_urls helper, deepen subcommand registration"
      contains: "def cmd_deepen"
    - path: "tests/test_researcher/test_cli.py"
      provides: "Unit tests for all cmd_deepen behaviors"
      contains: "test_cmd_deepen"
    - path: ".claude/skills/researcher/SKILL.md"
      provides: "Pass 2 workflow documentation"
      contains: "Pass 2"
  key_links:
    - from: ".claude/skills/researcher/scripts/researcher/cli.py"
      to: "source_manifest.json"
      via: "json.loads(manifest_path.read_text()) in _collect_deep_dive_urls"
      pattern: "source\\.get\\(\"verdict\"\\).*recommended"
    - from: ".claude/skills/researcher/scripts/researcher/cli.py"
      to: "pass2_NNN.json files"
      via: "write_text in cmd_deepen fetch loop"
      pattern: "pass2_.*\\.json"
    - from: ".claude/skills/researcher/scripts/researcher/cli.py"
      to: "fetcher.fetch_with_retry"
      via: "import and call in fetch loop"
      pattern: "fetch_with_retry\\(url\\)"
---

<objective>
Implement the `cmd_deepen` CLI subcommand that reads Claude's annotated source manifest and fetches targeted primary sources (Pass 2 of the two-pass research architecture).

Purpose: Complete the two-pass research pipeline so that after broad survey (Pass 1) and evaluation, users can run a second command to fetch deep-dive primary sources identified during evaluation.
Output: Working `deepen` subcommand in cli.py, comprehensive tests, updated SKILL.md workflow.
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deep-dive-pass/09-CONTEXT.md
@.planning/phases/09-deep-dive-pass/09-RESEARCH.md
@.planning/phases/09-deep-dive-pass/09-VALIDATION.md

@.claude/skills/researcher/scripts/researcher/cli.py
@.claude/skills/researcher/scripts/researcher/tiers.py
@.claude/skills/researcher/scripts/researcher/fetcher.py
@.claude/skills/researcher/scripts/researcher/url_builder.py
@.claude/skills/researcher/SKILL.md
@tests/test_researcher/test_cli.py

<interfaces>
<!-- Key functions the executor needs from existing modules -->

From cli.py (reuse as-is):
```python
def _strip_wiki_noise(markdown: str) -> str: ...
def _print_summary_table(sources: list[dict]) -> None: ...
def _get_tier_from_url(url: str) -> int: ...
def cmd_survey(topic: str) -> None: ...
def main() -> None: ...  # argparse dispatch — add deepen subcommand here
```

From url_builder.py (reuse as-is):
```python
def _get_project_root() -> Path: ...
def resolve_output_dir(root: Path, topic: str) -> Path: ...
```

From fetcher.py (reuse as-is):
```python
def fetch_with_retry(url: str) -> dict: ...
# Returns: {"success": bool, "content": str, "error": str, "fetch_status": str, "attempts_used": int}
```

From tiers.py (reuse as-is):
```python
def classify_domain(url_or_domain: str) -> int: ...  # Returns 1, 2, or 3
```

source_manifest.json schema (after Pass 1 evaluation):
```json
{
    "topic": "...",
    "run_timestamp": "...",
    "sources": [
        {
            "index": 1, "filename": "src_001.json", "url": "...", "domain": "...",
            "tier": 1, "word_count": 4523, "fetch_status": "ok",
            "evaluation_notes": "...",
            "deep_dive_urls": ["https://vault.fbi.gov/jonestown"],
            "verdict": "recommended"
        }
    ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write cmd_deepen test stubs covering all RSRCH-03 behaviors</name>
  <files>tests/test_researcher/test_cli.py</files>
  <behavior>
    - test_cmd_deepen_reads_recommended_only: Create manifest with 2 sources (one "recommended" with deep_dive_urls, one "skip"). Mock fetch_with_retry. Assert only the recommended source's URLs are fetched (check fetch_with_retry call args).
    - test_cmd_deepen_writes_pass2_json: Create manifest with 1 recommended source + 1 deep_dive_url. Mock fetch. Assert pass2_001.json exists with correct schema keys (index, url, domain, tier, word_count, fetch_status, error, content).
    - test_cmd_deepen_updates_manifest_pass2_sources: Same setup. Assert source_manifest.json now has "pass2_sources" key after cmd_deepen runs.
    - test_cmd_deepen_budget_guard: Create output_dir with 14 existing src_*.json files. Create manifest with 3 deep_dive_urls. Assert only 1 is fetched (15 - 14 = 1 budget). Assert budget-skipped URLs are printed.
    - test_cmd_deepen_dedup_pass1_urls: Create src_001.json with url="https://example.com/page". Create manifest with deep_dive_urls containing that same URL. Assert fetch_with_retry is NOT called for that URL.
    - test_cmd_deepen_dedup_across_sources: Create manifest with 2 recommended sources, both listing the same deep_dive_url. Assert fetch_with_retry called once for that URL.
    - test_cmd_deepen_clean_exit_no_urls: Create manifest with no deep_dive_urls (or only "skip" verdicts). Assert stdout contains "No deep-dive URLs found in manifest".
    - test_cmd_deepen_tier3_filtered: Create manifest with recommended source whose deep_dive_urls contains a Tier 3 domain (e.g., facebook.com). Assert that URL is not fetched.
    - test_cmd_deepen_cleans_old_pass2_files: Create existing pass2_001.json in output_dir. Run cmd_deepen. Assert old file is replaced (or removed if no URLs to fetch).
    - test_cmd_deepen_missing_verdict_treated_as_skip: Create manifest with source that has deep_dive_urls but no "verdict" key. Assert those URLs are NOT collected.
    - test_cmd_deepen_summary_table_printed: Run cmd_deepen with 1 fetchable URL. Assert stdout contains "Domain" and "Status" column headers.
  </behavior>
  <action>
    Add test functions to the existing `tests/test_researcher/test_cli.py` file, below the existing Phase 8 tests. Follow the same mocking pattern established by existing tests:
    - Patch `researcher.cli.resolve_output_dir` to return tmp_path subdirectory
    - Patch `researcher.cli._get_project_root` to return tmp_path
    - Patch `researcher.cli.fetch_with_retry` to return mock result dicts
    - Use `capsys` for stdout capture where needed

    Import `cmd_deepen` at the top alongside existing `cmd_survey` import:
    ```python
    from researcher.cli import cmd_survey, cmd_deepen
    ```

    For tests that need a manifest file, write it to `output_dir / "source_manifest.json"` using json.dumps before calling cmd_deepen.

    For tests that need existing src_*.json files (budget guard, dedup), create minimal JSON files with at least the "url" key.

    All tests should run without crawl4ai installed (the module-level mock handles this).

    Do NOT implement cmd_deepen yet — tests should fail (RED phase). The import of cmd_deepen will cause an ImportError until Task 2, so use a conditional import guard or add a minimal stub (just `def cmd_deepen(topic: str) -> None: raise NotImplementedError`) at the bottom of cli.py to allow tests to import. The stub counts as part of this task.
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/test_cli.py -k deepen -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 test_cmd_deepen_* tests exist in test_cli.py. Tests either fail (NotImplementedError from stub) or fail with assertion errors — confirming RED phase. No import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cmd_deepen, register subcommand, update SKILL.md</name>
  <files>.claude/skills/researcher/scripts/researcher/cli.py, .claude/skills/researcher/SKILL.md</files>
  <action>
    **In cli.py**, replace the NotImplementedError stub with the full implementation. Add these module-level private functions and modify main():

    1. **`_collect_deep_dive_urls(manifest_path: Path) -> list[str]`** — Read source_manifest.json, iterate sources where `verdict == "recommended"` (treat missing verdict as skip), collect all `deep_dive_urls` entries, deduplicate preserving manifest order, filter Tier 3 via `classify_domain`. Return list of URL strings.

    2. **`_get_fetched_urls(output_dir: Path) -> set[str]`** — Glob `src_*.json` files in output_dir, read each, collect `url` field into a set. Silently skip files that fail to parse.

    3. **`cmd_deepen(topic: str) -> None`** — Full implementation following the skeleton in 09-RESEARCH.md:
       - Resolve output_dir via `resolve_output_dir(_get_project_root(), topic)`
       - Check `manifest_path.exists()` — if missing, print error to stderr and `sys.exit(1)`
       - Delete existing `pass2_*.json` files (re-run cleanup per Pitfall 5)
       - Call `_collect_deep_dive_urls(manifest_path)` to get URL list
       - Call `_get_fetched_urls(output_dir)` and filter out already-fetched URLs
       - Budget guard: `pass1_count = len(list(output_dir.glob("src_*.json")))`, `pass2_budget = 15 - pass1_count`. If budget <= 0, print message and return.
       - Clean exit if no URLs remain after filtering: print "No deep-dive URLs found in manifest -- skip Pass 2 or re-evaluate sources" and return
       - Log budget-skipped URLs if `len(deep_dive_urls) > pass2_budget`, then truncate list
       - Fetch loop: for each URL, call `fetch_with_retry(url)`, apply `_strip_wiki_noise` to content, compute word_count, extract domain. Print progress. Write `pass2_{idx:03d}.json` with same schema as src_NNN.json. Build pass2_sources list (lightweight entries without content).
       - Call `_print_summary_table(pass2_sources)` (reuse existing function)
       - Read manifest, add `pass2_sources` key, write back in-place
       - Print manifest path

    4. **In `main()`**, add deepen subparser after survey:
       ```python
       deepen_parser = subparsers.add_parser("deepen", help="Pass 2: fetch targeted primary sources from evaluated manifest")
       deepen_parser.add_argument("topic", help="Topic string (same as used for survey)")
       ```
       Add dispatch branch:
       ```python
       elif args.command == "deepen":
           try:
               cmd_deepen(args.topic)
           except ValueError as exc:
               print(f"Error: {exc}", file=sys.stderr)
               sys.exit(1)
       ```

    **In SKILL.md**, add after the existing "Workflow (Pass 1 -- Survey)" section:

    A new "Workflow (Pass 2 -- Deep Dive)" section with 3 steps:
    - Step 1: Confirm evaluation is complete (verdict/deep_dive_urls present on all sources)
    - Step 2: Run deepen command: `PYTHONPATH=.claude/skills/researcher/scripts python -m researcher deepen "Your Topic Here"`
    - Step 3: Handoff — after cmd_deepen completes, say: "Both passes complete. Run cmd_write to generate Research.md."

    Also update the Phase 9-10 planned modules table to mark `deepen` as implemented.

    **Anti-patterns to avoid:**
    - Do NOT delete src_*.json files at start of cmd_deepen (only delete pass2_*.json)
    - Do NOT add any LLM API calls — cmd_deepen is pure [DETERMINISTIC]
    - Do NOT import crawl4ai at module level — keep deferred import pattern
    - Do NOT use `_strip_wiki_noise` conditionally by domain — the 50% guard handles all cases
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/test_cli.py -k deepen -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All 11 test_cmd_deepen_* tests pass. cmd_deepen is callable via CLI: `python -m researcher deepen "topic"`. SKILL.md documents the Pass 2 workflow. Full test suite (not just -k deepen) still passes: `pytest tests/test_researcher/ -x --tb=short`.</done>
</task>

</tasks>

<verification>
1. All deepen-specific tests pass: `PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/test_cli.py -k deepen -x --tb=short`
2. Full researcher test suite still passes (no regressions): `PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/ -x --tb=short`
3. CLI help shows deepen subcommand: `PYTHONPATH=.claude/skills/researcher/scripts python -m researcher --help`
4. SKILL.md contains Pass 2 workflow section
</verification>

<success_criteria>
- cmd_deepen reads only "recommended" verdict sources' deep_dive_urls from source_manifest.json
- pass2_NNN.json files are written with correct schema, distinct from src_NNN.json
- Budget guard enforces total <= 15 source files across both passes
- URL dedup works against both Pass 1 files and within deep_dive_urls
- Tier 3 URLs in deep_dive_urls are filtered out
- Clean exit with informative message when no fetchable URLs remain
- source_manifest.json updated with pass2_sources key
- Summary table printed in same format as Pass 1
- Old pass2_*.json files cleaned on re-run
- SKILL.md documents the complete Pass 2 workflow
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/09-deep-dive-pass/09-01-SUMMARY.md`
</output>
