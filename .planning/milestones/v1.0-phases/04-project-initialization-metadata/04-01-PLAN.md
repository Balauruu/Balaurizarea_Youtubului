---
phase: 04-project-initialization-metadata
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/skills/channel-assistant/scripts/channel_assistant/project_init.py
  - tests/test_channel_assistant/test_project_init.py
autonomous: true
requirements:
  - OUTP-03
  - OUTP-06

must_haves:
  truths:
    - "init_project() creates projects/N. [Title]/ with correct sequential numbering"
    - "Scaffold subdirectories research/, assets/, script/ exist inside the project directory"
    - "metadata.md is written with title variants table, description, and topic brief sections"
    - "Selected topic is auto-appended to past_topics.md in the canonical format that _load_past_topics() can parse"
    - "Windows forbidden characters are stripped from directory names"
  artifacts:
    - path: ".claude/skills/channel-assistant/scripts/channel_assistant/project_init.py"
      provides: "init_project(), load_project_inputs(), _next_project_number(), _sanitize_dir_name(), _create_scaffold(), _append_past_topic(), _write_metadata()"
      exports: ["init_project", "load_project_inputs"]
    - path: "tests/test_channel_assistant/test_project_init.py"
      provides: "Unit tests for all deterministic project init behaviors"
      min_lines: 80
  key_links:
    - from: ".claude/skills/channel-assistant/scripts/channel_assistant/project_init.py"
      to: "projects/N. Title/metadata.md"
      via: "_write_metadata() called from init_project()"
      pattern: "path\\.write_text"
    - from: ".claude/skills/channel-assistant/scripts/channel_assistant/project_init.py"
      to: "context/channel/past_topics.md"
      via: "_append_past_topic() called from init_project()"
      pattern: "f\\.write.*\\*\\*.*\\*\\*"
---

<objective>
Create the project_init.py module with TDD: deterministic functions for project directory creation, metadata file writing, and past_topics.md dedup loop closure.

Purpose: OUTP-03 (project directory creation with sequential numbering) and OUTP-06 (metadata.md output) depend entirely on this module. All deterministic logic lives here; heuristic generation (title variants, description) is handled externally by Claude and passed in as structured data.

Output: Tested project_init.py module with init_project() as the primary entry point.
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-project-initialization-metadata/04-RESEARCH.md

@.claude/skills/channel-assistant/scripts/channel_assistant/topics.py
@.claude/skills/channel-assistant/scripts/channel_assistant/cli.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From topics.py:
```python
def _load_past_topics(path: Path) -> list[str]:
    """Canonical line format: ``- **Title** | YYYY-MM-DD | one-line summary``"""

def write_topic_briefs(briefs: list[dict], output_path: Path) -> None:
    """Brief section format: ## N. Title"""

def format_chat_cards(briefs: list[dict]) -> str:
    """Format: [N] Title -- hook"""
```

From cli.py:
```python
def _get_project_root() -> Path:
    """Find the project root (directory containing CLAUDE.md)."""
```
</interfaces>
</context>

<feature>
  <name>project_init module - deterministic project creation</name>
  <files>.claude/skills/channel-assistant/scripts/channel_assistant/project_init.py, tests/test_channel_assistant/test_project_init.py</files>
  <behavior>
    - _next_project_number(empty_dir) -> 1
    - _next_project_number(dir with "1. Title", "2. Title") -> 3
    - _next_project_number(dir with "1. Title", "3. Title") -> 4 (uses max, not count)
    - _next_project_number(nonexistent_dir) -> 1
    - _sanitize_dir_name('Title: The "Bad" One?') -> 'Title The Bad One'
    - _sanitize_dir_name('Clean Title') -> 'Clean Title' (no change)
    - _create_scaffold(project_dir) creates research/, assets/, script/ subdirs
    - _append_past_topic writes "- **Title** | YYYY-MM-DD | hook" format
    - _append_past_topic output round-trips through _load_past_topics() (import from topics.py)
    - _write_metadata creates file with Title Variants table, YouTube Description section, Topic Brief section
    - _write_metadata includes RECOMMENDED label on the flagged variant
    - _write_metadata includes all 5 variants in table rows
    - init_project() creates numbered dir, scaffold, metadata.md, appends past_topics.md, returns project_dir Path
    - init_project() with title_variants containing a >70 char title logs a warning (does not fail)
    - load_project_inputs(root, topic_number) raises FileNotFoundError if topic_briefs.md missing
    - load_project_inputs returns dict with "brief_markdown" and "title_patterns" keys
  </behavior>
  <implementation>
    Create project_init.py with these functions following the exact signatures and patterns from 04-RESEARCH.md:

    Internal helpers:
    - _next_project_number(projects_dir: Path) -> int: regex scan of "N." prefix dirs, return max+1
    - _sanitize_dir_name(title: str) -> str: re.sub(r'[<>:"/\\|?*]', '', title).strip()
    - _create_scaffold(project_dir: Path) -> None: mkdir research/, assets/, script/
    - _append_past_topic(path: Path, title: str, hook: str) -> None: append canonical format line
    - _write_metadata(path, title, variants, description, brief_markdown) -> None: write markdown with table

    Public API:
    - init_project(root, title, hook, title_variants, description, brief_markdown) -> Path
    - load_project_inputs(root, topic_number) -> dict with brief_markdown and title_patterns

    All stdlib only (pathlib, re, datetime). No new dependencies.
    Import _load_past_topics from topics.py only in tests for round-trip verification.
  </implementation>
</feature>

<verification>
pytest tests/test_channel_assistant/test_project_init.py -x -v
pytest tests/ -x (full suite still green)
</verification>

<success_criteria>
- All test_project_init.py tests pass
- init_project() creates correct directory structure with sequential numbering
- metadata.md contains variants table with RECOMMENDED label, description, and brief sections
- past_topics.md append round-trips through _load_past_topics()
- Windows forbidden characters stripped from directory names
- Full test suite (pytest tests/) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-initialization-metadata/04-01-SUMMARY.md`
</output>
