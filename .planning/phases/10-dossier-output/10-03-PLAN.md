---
phase: 10-dossier-output
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - .claude/skills/researcher/SKILL.md
autonomous: false
requirements: [DOSS-01, DOSS-02, DOSS-03, DOSS-04, DOSS-05, DOSS-06, DOSS-07, DOSS-08, DOSS-09, MEDIA-01, MEDIA-02]

must_haves:
  truths:
    - "cmd_write produces synthesis_input.md from real project source files"
    - "Claude can read synthesis_input.md + synthesis.md and produce Research.md with all 9 sections"
    - "Claude produces media_urls.md grouped by asset type"
    - "Research.md is written to projects/N. [Title]/research/"
    - "Full researcher test suite passes after integration"
  artifacts:
    - path: "projects/*/research/Research.md"
      provides: "Writer-ready research dossier"
      contains: "HOOK:"
    - path: "projects/*/research/media_urls.md"
      provides: "Visual media catalog"
      contains: "Archival"
  key_links:
    - from: ".claude/skills/researcher/scripts/researcher/cli.py"
      to: "projects/*/research/synthesis_input.md"
      via: "cmd_write writes aggregated content"
      pattern: "synthesis_input\\.md"
    - from: ".claude/skills/researcher/prompts/synthesis.md"
      to: "projects/*/research/Research.md"
      via: "Claude reads prompt and produces dossier"
      pattern: "Research\\.md"
---

<objective>
Run end-to-end integration test with a real topic that has existing scraped data: execute cmd_write, then Claude synthesis, verify both output files match schema.

Purpose: Validates the complete pipeline works -- code aggregation feeds Claude synthesis, producing the final v1.1 deliverables (Research.md + media_urls.md). This is the milestone capstone.

Output: Research.md and media_urls.md in the project's research directory, verified by human review
</objective>

<execution_context>
@C:/Users/iorda/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/iorda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dossier-output/10-CONTEXT.md
@.planning/phases/10-dossier-output/10-01-SUMMARY.md
@.planning/phases/10-dossier-output/10-02-SUMMARY.md

@.claude/skills/researcher/SKILL.md
@.claude/skills/researcher/prompts/synthesis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cmd_write on existing project and execute synthesis</name>
  <files></files>
  <action>
    1. Find an existing project directory that has src_*.json files (from a previous cmd_survey run):
       ```bash
       ls projects/*/research/src_*.json 2>/dev/null | head -5
       ```
       If no project exists with source files, use the Duplessis Orphans project (the only past topic per past_topics.md). If that also has no source files, run cmd_survey first to generate them.

    2. Run cmd_write:
       ```bash
       PYTHONPATH=.claude/skills/researcher/scripts python -m researcher write "The Topic Name"
       ```
       Verify it prints: source counts, synthesis_input.md path, synthesis prompt path, instruction line.

    3. Read synthesis_input.md -- verify it contains source headers with URL/domain/tier/words and full content.

    4. Read the synthesis prompt at .claude/skills/researcher/prompts/synthesis.md.

    5. Following the synthesis prompt instructions, produce Research.md and media_urls.md. Write both to the output directory shown in synthesis_input.md header.

    6. Run full test suite to confirm nothing is broken:
       ```bash
       PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/ -x --tb=short --ignore=tests/test_researcher/test_integration.py
       ```
  </action>
  <verify>
    <automated>cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/ -x --tb=short --ignore=tests/test_researcher/test_integration.py</automated>
  </verify>
  <done>cmd_write ran successfully. synthesis_input.md created. Research.md and media_urls.md written to project research directory. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Research.md and media_urls.md quality</name>
  <action>
    Claude has produced Research.md and media_urls.md via the synthesis prompt. Human reviews both files for schema compliance and content quality.
  </action>
  <verify>Human confirms output quality</verify>
  <done>Research.md has all 9 sections in correct order with HOOK/QUOTE callouts. media_urls.md has entries grouped by asset type. Human approved.</done>
  <what-built>Complete researcher pipeline: cmd_survey (Pass 1) + cmd_deepen (Pass 2) + cmd_write (aggregation) + Claude synthesis (Research.md + media_urls.md). This is the final v1.1 milestone deliverable.</what-built>
  <how-to-verify>
    1. Open the Research.md file in the project's research/ directory
    2. Verify these sections exist in order: Subject Overview, Timeline, Key Figures, Narrative Hooks, Direct Quotes, Contradictions, Unanswered Questions, Correcting the Record, Source Credibility
    3. Check that HOOK callouts use format: **HOOK: [Title]** followed by 1-2 sentences
    4. Check that QUOTE callouts use format: **QUOTE: [Label]** followed by blockquote and attribution
    5. Check Source Credibility table has columns: Source, Type, Corroborated By, Access Quality (no scalar scores)
    6. Rough word count: body should be approximately 2,000 words
    7. Open media_urls.md -- verify entries are grouped by Archival Footage / Archival Photos / Documents / B-Roll with URL + Description + Source per entry
    8. Verify both files are in `projects/N. [Title]/research/` directory
  </how-to-verify>
  <resume-signal>Type "approved" if Research.md and media_urls.md look correct, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
# Full test suite
cd "D:/Youtube/D. Mysteries Channel/1.2 Agents/Channel-automation V3" && PYTHONPATH=.claude/skills/researcher/scripts pytest tests/test_researcher/ -x --tb=short --ignore=tests/test_researcher/test_integration.py

# Research.md exists and has key sections
ls projects/*/research/Research.md 2>/dev/null
grep -c "HOOK:\|QUOTE:\|Timeline\|Contradictions" projects/*/research/Research.md 2>/dev/null

# media_urls.md exists and has asset groups
ls projects/*/research/media_urls.md 2>/dev/null
grep -c "Archival\|B-Roll\|Documents" projects/*/research/media_urls.md 2>/dev/null
```
</verification>

<success_criteria>
- cmd_write runs without error on real project data
- Research.md contains all 9 sections in correct order with HOOK/QUOTE callouts
- media_urls.md contains visual media URLs grouped by asset type
- Both files written to projects/N. [Title]/research/
- Human approves output quality
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-dossier-output/10-03-SUMMARY.md`
</output>
